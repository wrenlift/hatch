name        = "@hatch:url"
version     = "0.1.0"
entry       = "url"
description = "URL parser, builder, and percent-encoder."

[spec-dependencies]
"@hatch:test"   = { path = "../hatch-test" }
"@hatch:assert" = { path = "../hatch-assert" }
