name        = "@hatch:random"
version     = "0.1.0"
entry       = "random"
description = "Random numbers, sampling, shuffle — one-shot statics + seedable streams."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"