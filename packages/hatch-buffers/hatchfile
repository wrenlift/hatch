name        = "@hatch:buffers"
version     = "0.1.3"
entry       = "buffers"
description = "Spec coverage for the built-in ByteArray / Float32Array / Float64Array types."
homepage    = "https://hatch.wrenlift.com/packages/@hatch:buffers"
readme   = "README.md"
changelog = "CHANGELOG.md"

[spec-dependencies]
"@hatch:test" = "0.2.1"
"@hatch:assert" = "0.2.3"
"@hatch:crypto" = "0.2.2"
"@hatch:zip" = "0.1.2"