name        = "@hatch:proc"
version     = "0.3.0"
entry       = "proc"
description = "Subprocess spawn with lifecycle, streaming IO (blocking + fiber-cooperative), and pipelines."

[dependencies]
"@hatch:io" = "0.2.0"
[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"