name        = "@hatch:fmt"
version     = "0.1.0"
entry       = "fmt"
description = "ANSI color + padding + small numeric helpers for terminal output."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"