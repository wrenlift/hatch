name        = "@hatch:regex"
version     = "0.1.0"
entry       = "regex"
description = "Compiled regular expressions with capture groups, replace, and split."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"