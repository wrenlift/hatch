name        = "@hatch:events"
version     = "0.2.1"
entry       = "events"
description = "Signals, EventEmitter, and a cooperative Scheduler for fiber concurrency."
homepage    = "https://hatch.wrenlift.com/packages/@hatch:events"
readme   = "README.md"

[spec-dependencies]
"@hatch:test"   = { path = "../hatch-test" }
"@hatch:assert" = { path = "../hatch-assert" }