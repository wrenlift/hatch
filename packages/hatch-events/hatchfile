name        = "@hatch:events"
version     = "0.2.0"
entry       = "events"
description = "Signals, EventEmitter, and a cooperative Scheduler for fiber concurrency."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"