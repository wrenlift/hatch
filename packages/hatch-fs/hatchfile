name        = "@hatch:fs"
version     = "0.1.0"
entry       = "fs"
description = "Filesystem I/O — read, write, stat, list, walk."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"
"@hatch:path"   = "0.1.0"