name        = "@hatch:time"
version     = "0.1.0"
entry       = "time"
description = "Clocks, sleep, and UTC timestamp formatting."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"