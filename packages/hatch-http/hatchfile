name        = "@hatch:http"
version     = "0.3.0"
entry       = "http"
description = "HTTP/1.1 client with TLS, streaming bodies, and fiber-cooperative reads."

[dependencies]
"@hatch:json" = "0.1.0"
"@hatch:url"  = "0.1.0"
"@hatch:io"   = "0.2.0"
[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"