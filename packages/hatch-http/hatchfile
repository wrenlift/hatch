name        = "@hatch:http"
version     = "0.3.0"
entry       = "http"
description = "HTTP/1.1 client with TLS, streaming bodies, and fiber-cooperative reads."

[dependencies]
"@hatch:json" = { path = "../hatch-json" }
"@hatch:url"  = { path = "../hatch-url" }
"@hatch:io"   = { path = "../hatch-io" }

[spec-dependencies]
"@hatch:test"   = { path = "../hatch-test" }
"@hatch:assert" = { path = "../hatch-assert" }
