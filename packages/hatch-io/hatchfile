name        = "@hatch:io"
version     = "0.2.0"
entry       = "io"
description = "Byte buffers plus Reader / Writer streams with fiber-cooperative `withTryFn`."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"