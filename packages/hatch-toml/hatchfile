name        = "@hatch:toml"
version     = "0.1.0"
entry       = "toml"
description = "TOML parser and serializer, backed by the Rust `toml` crate."

[spec-dependencies]
"@hatch:test"   = "0.1.0"
"@hatch:assert" = "0.2.0"