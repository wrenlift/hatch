name        = "@hatch:socket"
version     = "0.1.1"
entry       = "socket"
description = "TCP listeners + connections and UDP datagram sockets. Blocking and non-blocking variants suitable for fiber-cooperative schedulers."
homepage    = "https://hatch.wrenlift.com/packages/@hatch:socket"
readme   = "README.md"

[spec-dependencies]
"@hatch:test"   = { path = "../hatch-test" }
"@hatch:assert" = { path = "../hatch-assert" }