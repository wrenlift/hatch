name        = "@hatch:zip"
version     = "0.1.2"
entry       = "zip"
description = "Read and write ZIP archives entirely in memory (store / deflate / zstd)."
homepage    = "https://hatch.wrenlift.com/packages/@hatch:zip"
readme   = "README.md"

[spec-dependencies]
"@hatch:test" = "0.2.1"
"@hatch:assert" = "0.2.3"