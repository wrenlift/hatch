name        = "hud-gamepad"
version     = "0.1.0"
entry       = "main"
description = "Three-button pause menu navigable by both mouse and gamepad. DPad / left-stick moves focus; A presses. Showcases the HUD's per-frame focus model + the @hatch:game Input layer's gamepadJustPressed surface."

[dependencies]
"@hatch:game" = { path = "../../../packages/hatch-game" }
"@hatch:hud"  = { path = "../../../packages/hatch-hud" }
