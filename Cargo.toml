[workspace]
resolver = "2"
members = ["cli"]

# Official hatch packages under `packages/` are Wren source trees, not
# Cargo crates — they're not workspace members. `hatch-cli pack` walks
# them into `.hatch` binaries just like any user package.
